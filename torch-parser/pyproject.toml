[project]
name = "torch-parser"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []


[tool.hatch.build.targets.wheel]
packages = ["src/torch_parser"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

